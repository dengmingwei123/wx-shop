@import './stylus/iconfont.wxss';

page,
view,
text,
swiper,
swiper-item,
image,navigator {
  margin:0;
  padding:0;
  box-sizing:border-box;
}

page {
  /* 定义主题颜色 */
  --themeColor:#eb4450;
  /* 定义统一的字体大小 */
  font-size:28rpx
}
